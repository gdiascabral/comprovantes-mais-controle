# Dependências só de desenvolvimento (não entram no exe).
pytest>=8,<9
vermin>=1.6,<2
ruff>=0.6,<1
pytest-cov>=5,<6
# Quem compila o requirements.lock. Faixa mais estreita que as outras de
# propósito: o CI recompila o lock e compara com `git diff --exit-code`,
# então uma mudança de FORMATO da saída do uv derrubaria a build sem que
# nenhuma dependência tivesse mudado. Prender a série 0.12 é o que separa
# "alguém esqueceu de recompilar" de "o uv passou a escrever diferente".
uv>=0.12,<0.13
