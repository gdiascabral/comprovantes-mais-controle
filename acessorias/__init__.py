# -*- coding: utf-8 -*-
"""Envio do fechamento mensal ao escritório contábil, pelo portal Acessórias.

Pacote importado por caminho completo (`from acessorias.frame import
AcessoriasFrame`), como todas as outras pastas do repositório desde 02/09/2026.
Ser pacote é o que dispensa o prefixo que o `extratos_sicoob/` precisou usar
quando nome de módulo ainda era global no `sys.path` do app: aqui o `config.py`
é `acessorias.config` e não disputa nada com o do Anexar.
"""
